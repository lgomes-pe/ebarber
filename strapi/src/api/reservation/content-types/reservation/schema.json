{
  "kind": "collectionType",
  "collectionName": "reservations",
  "info": {
    "singularName": "reservation",
    "pluralName": "reservations",
    "displayName": "Reservation"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "client": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::client.client",
      "inversedBy": "reservations"
    },
    "prestation": {
      "type": "relation",
      "relation": "oneToOne",
      "target": "api::prestation.prestation"
    },
    "date": {
      "type": "datetime",
      "required": true
    },
    "employee": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::employee.employee",
      "inversedBy": "reservations"
    },
    "reservationState": {
      "type": "enumeration",
      "enum": [
        "pending",
        "accepted",
        "canceled"
      ],
      "required": true
    }
  }
}
